import React from "react";
import Container from "react-bootstrap/Container";

const ServicesFrame = () => {
  return (
    <Container
      fluid
      style={{
        minHeight: "65vh",
      }}
    >
      Services
    </Container>
  );
};

export default ServicesFrame;
